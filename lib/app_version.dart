class AppVersion {
  // version format: {major}{minor}{patch}.{build}
  static const double version = 100.5;

  // static double? get iosVersion => AppConfig.metaData?.appAndroidVersion;

  // static double? get androidVersion => AppConfig.metaData?.appIosVersion;
}
