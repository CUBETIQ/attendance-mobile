import 'package:flutter/material.dart';

class SummaryAttendaceViewTablet extends StatelessWidget {
  const SummaryAttendaceViewTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
