import 'package:flutter/material.dart';

class NavigationViewTablet extends StatelessWidget {
  const NavigationViewTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
