import 'package:flutter/material.dart';

class EditProfileViewTablet extends StatelessWidget {
  const EditProfileViewTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text("Edit Profile Tablet"),
      ),
    );
  }
}
