import 'package:flutter/material.dart';

class SettingViewTablet extends StatelessWidget {
  const SettingViewTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
