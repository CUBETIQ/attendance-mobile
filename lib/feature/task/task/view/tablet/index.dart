import 'package:flutter/material.dart';

class TaskViewTablet extends StatelessWidget {
  const TaskViewTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
